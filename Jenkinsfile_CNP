#!groovy

@Library("Infrastructure@feature/add-build-info")

def product = "cmc"
def component = "pdf-service"

withPipeline("java", product, component) {
  after('checkout') {
    echo 'pdf-service checked out'
  }
}
