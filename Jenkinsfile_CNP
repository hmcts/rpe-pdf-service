#!groovy

@Library("Infrastructure@feature/retry-for-healthcheck")

def product = "cmc"
def component = "pdf-service"

withPipeline("java", product, component) {
  enableSlackNotifications('#cmc-tech-cnp')
}
