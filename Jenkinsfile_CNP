#!groovy

@Library("Infrastructure@feature/Enable-deployments-for-cnp-branch")

def product = "cmc"
def component = "pdf-service"

withPipeline("java", product, component) {
  enableSlackNotifications('#cmc-tech-notification')
}
