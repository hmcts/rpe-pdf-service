apply plugin: 'java'

dependencies {
  compile group: 'org.springframework', name: 'spring-web', version: rootProject.versions.spring
  testCompile group: 'org.springframework.boot', name: 'spring-boot-starter-test', version: project.versions.springBoot
}
